import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#0C296B", // Bleu
    },
    secondary: {
      main: "#EA4949", // Rouge
    },
  },
});

export default theme;
